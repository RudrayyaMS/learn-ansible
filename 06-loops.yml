- name: Loops Demo
  hosts: localhost
  tasks:
    - name: Print Fruit Name
      debug:
        msg: Fruit Name - {{ item }}
      loop:
        - Apple
        - Banana
        - Orange

# for i in apple bana ; do echo $i
# in ansible we have to use {{ item }}